const calcStore = async (a, b, result) => {
    // Call backend API and store the results
    // await fetch('http://localhost:10000/store')
    //         .then(response => response.json())
    //         .catch(error => {throw(error);});
    return true;
};

module.exports = {calcStore};